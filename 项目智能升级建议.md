# 考研小管家：从"LLM 壳子"到"智能学习助手"的升级建议

## 背景

老师指出传统 MIS 项目的五大弊病（功能同质化、纯 CRUD、重 UI 轻核心、流程僵化、数据孤岛），并给出对比框架：

| 维度 | 传统 MIS | 智能应用 |
|------|----------|----------|
| 核心角色 | 被动记录工具 | 主动助理 |
| 系统能力 | 仅存储数据 | 交流 + 记忆 + 知识 + 技能 |
| 完整链路 | 记录→存储 | 记录→分析→推荐→预警 |
| 用户体验 | 用完即走 | 越用越懂用户 |

对照当前项目现状：

- **交流**：有，但所有 handler 本质都是"构造 prompt → 调 LLM → 解析返回"，是 LLM 的薄壳
- **记忆**：伪记忆。服务器无状态，localStorage 每次重建为文本快照，LLM 看到的不是"记忆"而是"摘要"
- **知识**：有，但仅限 LLM 自身知识，无考研领域结构化知识库
- **技能**：无。没有算法能力（间隔重复、自适应规划、弱点推断），所有"分析"都是让 LLM 编文字

---

## 核心诊断：数据孤岛问题最严重

项目收集了大量数据（学习记录、计划完成率、错题本、番茄钟），但这些数据**从未被交叉分析**：

- 错题多的科目 → 不会自动增加学习时间
- 计划完成率低的时段 → 不会调整计划结构
- 长期未复习的科目 → 只有前端提醒，不会影响计划生成
- 学习时长趋势 → 只在图表展示，不会驱动任何决策

---

## 具体建议（按实现难度排序）

### 第一阶段：让数据流动起来（改造现有功能，难度低）

#### 1. 自适应计划生成 — 改造 `src/llm/plan.ts`

**当前问题**：`plan_create` 每次让 LLM 凭空生成计划，不考虑任何历史数据。

**改法**：前端发送结构化统计数据（而非文本摘要）给后端，plan prompt 中注入：

- 近 7 天各科目实际学习时长 vs 计划时长的偏差
- 各科目计划完成率
- 错题本中各科目的错误数量和类型分布
- 距考试天数和当前阶段
- 用户历史偏好（哪个时段完成率高、平均专注时长）

**效果**：从"LLM 随机生成"变成"基于数据的个性化推荐"，这本身就是老师说的"记录→分析→推荐"链路。

**改动范围**：
- `public/index.html`：新增 `getStudyAnalytics()` 函数，计算结构化统计数据
- `public/index.html`：修改 WebSocket 发送逻辑，附带 analytics 数据
- `src/index.ts`：接收并传递 analytics 数据
- `src/llm/plan.ts`：改造 `plan_create` 的 prompt，注入统计数据

#### 2. 错题-学习联动 — 改造 `src/llm/review.ts` + `src/llm/plan.ts`

**当前问题**：错题本和计划是完全独立的数据。

**改法**：
- 前端新增 `getSubjectAnalytics()` 函数，计算每个科目的：错题数、错误类型分布、最近一次学习距今天数、近 7 天学习时长
- 这个分析结果注入到 `plan_create` 的 prompt 中
- LLM 生成计划时自动给弱项科目分配更多时间

**效果**：打破数据孤岛，实现老师说的"越用越懂用户"。

**改动范围**：
- `public/index.html`：新增 `getSubjectAnalytics()` 函数
- `src/llm/plan.ts`：prompt 中加入科目分析数据，指导时间分配

#### 3. 真正的间隔复习引擎 — 新增 `src/llm/spaced-repetition.ts`

**当前问题**：`review_schedule` 只是让 LLM 编一段关于遗忘曲线的文字，没有真正实现。

**改法**：在前端实现一个简单的 SM-2 算法：

```typescript
// 知识点复习状态
interface ReviewItem {
  topic: string;           // 知识点名称
  subject: string;         // 所属科目
  firstLearnDate: string;  // 首次学习时间
  lastReviewDate: string;  // 上次复习时间
  reviewCount: number;     // 复习次数
  easeFactor: number;      // 难度系数（初始 2.5）
  interval: number;        // 当前间隔天数
  nextReviewDate: string;  // 下次复习日期
}
```

- 每个学过的知识点记录：首次学习时间、上次复习时间、复习次数、难度系数（EF）
- 根据遗忘曲线计算下次复习时间
- 在每日提醒中推送"今天需要复习的知识点"
- `review_schedule` 不再问 LLM，而是返回算法计算结果

**效果**：从"LLM 编故事"变成真正的算法驱动，这是老师说的"技能"。

---

### 第二阶段：从被动到主动（新增功能，难度中）

#### 4. 学习模式分析 — 新增 `src/llm/analytics.ts`

基于已有的学习记录和番茄钟数据，计算：

- **最佳学习时段**：哪个时间段的专注时长最长
- **平均专注周期**：用户实际能专注多久，而非固定 25 分钟
- **各科目单位时间产出**：结合计划完成率和学习时长
- **学习疲劳检测**：连续多天高强度后的效率下降趋势

将这些分析结果作为"用户画像"注入到所有 LLM prompt 中。

#### 5. 智能预警系统 — 扩展前端 `checkReminders()`

当前的提醒是简单的规则判断（3 天没学某科目）。升级为：

- **进度预警**：根据当前学习速度和剩余时间，计算"按这个速度能否覆盖所有考点"
- **偏科预警**：某科目学习时长占比远低于其在考试中的分值占比
- **效率预警**：某科目学习时长增加但错题率没有下降
- **心理预警**：连续多天学习时长骤降、计划完成率暴跌

#### 6. 阶段自适应 — 改造 `src/llm/review.ts` 的 `review_stage`

**当前问题**：根据考试日期给固定建议。

**改法**：根据实际进度动态调整：

- 基础阶段：如果某科目的错题集中在概念性错误 → 建议回归教材
- 强化阶段：如果计算错误率高 → 建议增加练习量
- 冲刺阶段：根据知识覆盖率推荐重点复习方向

---

### 第三阶段：架构升级（难度高，可作为课程设计亮点）

#### 7. 服务器端持久化

将关键数据（学习记录、错题、计划完成历史）存到服务器（SQLite 即可），实现：

- 跨设备同步
- 服务端主动分析（不依赖用户打开页面）
- 历史趋势的结构化查询（而非每次从 localStorage 重建文本）

#### 8. 知识图谱

为考研四科构建简单的知识依赖图：

- 数学：高数 → 线代 → 概率论，各章节有前置依赖
- 当用户在某知识点频繁出错时，自动推荐其前置知识点复习
- 可视化知识掌握度热力图

---

## 对照老师框架的改进映射

| 老师的维度 | 当前状态 | 改进后 |
|-----------|---------|--------|
| 核心角色 | LLM 壳子 + CRUD | 数据驱动的主动助手 |
| 用户行为 | 手动记录 + 问 LLM | 系统主动推荐 + 预警 |
| 系统能力 | 仅交流（LLM） | 交流 + 真实记忆 + 算法技能 |
| 完整链路 | 记录→存储→问LLM | 记录→分析→推荐→预警→自适应 |
| 用户体验 | 每次重新开始 | 越用越精准 |

---

## 实施建议

**优先做第一阶段的 1 和 2**（自适应计划 + 错题联动），这两个改动量最小但最能体现"智能"：

- 不需要新架构
- 只需要改造前端数据打包 + 后端 prompt 构造
- 直接打破数据孤岛
- 演示时能清晰展示"系统基于用户数据做决策"

**然后做 3（间隔复习）**，这是一个独立的算法模块，容易展示技术深度。

---

## 关键文件清单

| 文件 | 改动内容 |
|------|---------|
| `public/index.html` | 新增 `getStudyAnalytics()`、`getSubjectAnalytics()`，改造 WebSocket 发送逻辑 |
| `src/llm/plan.ts` | 改造 `plan_create` prompt，注入统计数据 |
| `src/llm/review.ts` | 改造 `review_stage`，关联实际数据 |
| `src/llm/spaced-repetition.ts` | **新增**，SM-2 间隔重复算法 |
| `src/llm/analytics.ts` | **新增**，学习模式分析 |
| `src/index.ts` | 接收并传递 analytics 数据 |
