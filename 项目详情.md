# 考研小管家 - 项目详情

## 一、项目概述

| 维度 | 内容 |
|------|------|
| 项目名称 | 考研小管家（Kaoyan Butler） |
| 目标用户 | 考研备考学生（大三/大四/二战考生） |
| 核心价值 | "聊天即备考"——用自然语言管理考研四科复习 |

### 解决的问题

考研学生面临四科（政治/英语/数学/专业课）复习平衡困难、计划混乱、知识点遗忘、心态焦虑等痛点。考研小管家通过AI对话的方式，让学生用自然语言完成学习记录、计划制定、知识问答、情绪管理，降低备考管理的认知负担。

---

## 二、技术架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────┐
│                  前端 (浏览器)                │
│  HTML/CSS/JS · LocalStorage · WebSocket     │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐│
│  │ 聊天界面  │ │ 计划面板  │ │ 记忆管理     ││
│  └──────────┘ └──────────┘ └──────────────┘│
└──────────────────┬──────────────────────────┘
                   │ WebSocket (JSON)
┌──────────────────┴──────────────────────────┐
│                后端 (Node.js)                │
│  Express + WebSocketServer + TypeScript      │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐│
│  │ 意图识别  │ │ 计划生成  │ │ DeepSeek API ││
│  │ (三层)    │ │ (plan.ts)│ │ (deepseek.ts)││
│  └──────────┘ └──────────┘ └──────────────┘│
└─────────────────────────────────────────────┘
```

### 2.2 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | HTML + CSS + JavaScript | 单文件SPA，无需构建 |
| 后端 | Node.js + Express + ws | TypeScript，ESM模块 |
| AI | DeepSeek API | deepseek-chat模型，OpenAI兼容格式 |
| 存储 | LocalStorage | 前端持久化：档案、记录、计划、对话 |
| 通信 | WebSocket | 实时双向通信，JSON消息格式 |
| 图表 | Chart.js 4.x | 前端数据可视化（CDN引入） |
| 环境 | dotenv | API Key管理 |

### 2.3 目录结构

```
kaoyan-butler/
├── public/
│   └── index.html              # 前端SPA（聊天界面+计划面板+统计面板+记忆管理）
├── src/
│   ├── index.ts                # 服务端入口（Express+WebSocket+意图路由）
│   ├── llm/
│   │   ├── types.ts            # LLM类型定义
│   │   ├── deepseek.ts         # DeepSeek API客户端
│   │   ├── prompt.ts           # Prompt模板（考研系统提示词+场景路由）
│   │   ├── plan.ts             # 计划生成与管理
│   │   ├── qa.ts               # 知识问答处理
│   │   ├── review.ts           # 提醒与复习建议
│   │   ├── emotion.ts          # 情绪关怀处理
│   │   ├── zhipu.ts            # 智谱API客户端（备用）
│   │   ├── intent/
│   │   │   ├── index.ts        # 意图处理流水线
│   │   │   ├── regex.ts        # 第一层：正则快速匹配
│   │   │   ├── ai.ts           # 第二层：AI意图识别
│   │   │   └── safety.ts       # 第三层：安全检查规则
│   │   └── memory/
│   │       ├── index.ts        # 记忆模块导出
│   │       ├── types.ts        # 记忆数据类型
│   │       └── storage.ts      # LocalStorage CRUD
├── test-intent.ts              # 意图识别单元测试（33用例）
├── test-ws.ts                  # WebSocket端到端测试（10用例）
├── 测试方案.md                  # 测试方案文档
├── 项目流程方案.md              # 12周开发计划
├── 项目详情.md                  # 本文档
├── package.json
├── tsconfig.json
└── .env                        # DeepSeek API Key
```

---

## 三、核心功能

### 3.1 三层意图识别系统

**设计思想**：短消息零成本匹配 → 复杂消息AI分类 → 全量安全过滤

#### 第一层：正则快速匹配

- 6类规则：问候、感谢、告别、帮助、否定、查看指令
- 精确匹配，消息长度≤15字
- 零API调用成本，毫秒级响应
- 文件：[regex.ts](src/llm/intent/regex.ts)

#### 第二层：AI意图识别

- 5大类、20+子意图的分类体系
- DeepSeek API调用，返回结构化JSON
- 提取用户数据（科目、时长、日期等）
- 文件：[ai.ts](src/llm/intent/ai.ts)

意图分类：

| 大类 | 子意图 | 示例 |
|------|--------|------|
| 学习记录 | study_log / study_query / study_break | "今天学了3小时高数" |
| 计划管理 | plan_create / plan_query / plan_modify / plan_complete | "帮我安排明天的复习" |
| 知识问答 | qa_ask / qa_explain / qa_example | "什么是泰勒展开？" |
| 复习提醒 | review_query / review_schedule / review_stage | "我高数哪些章节薄弱？" |
| 智能对话 | chat_greeting / chat_chat / chat_comfort / chat_encouragement / chat_help | "感觉来不及了" |

#### 第三层：安全检查规则

- 5类风险：身体不适、过度学习、考研焦虑、比较焦虑、自伤风险
- 3个风险级别：WARN（提醒）、URGENT（关怀）、BLOCK（拦截）
- 考研场景特化关键词（如"来不及了""想放弃""别人进度快"）
- 文件：[safety.ts](src/llm/intent/safety.ts)

#### 流水线

```
用户消息 → 第一层(正则) → 命中？→ 直接返回预设回复
                      ↓ 未命中
              第二层(AI意图) → 识别意图+提取数据
                      ↓
              第三层(安全检查) → 风险？→ 返回安全提示
                      ↓ 安全
              执行对应功能 → 返回结果
```

文件：[index.ts](src/llm/intent/index.ts)

### 3.2 长期记忆系统

**存储方案**：LocalStorage（浏览器端持久化，无需数据库）

#### 用户档案

| 字段 | 说明 | 提取方式 |
|------|------|---------|
| name | 姓名 | "我叫小明" / "我是小明" |
| targetSchool | 目标院校 | "考浙大" / "目标是北大" |
| targetMajor | 目标专业 | "计算机科学" |
| examDate | 考试日期 | "12月21号考试" |
| stage | 备考阶段 | 基础/强化/冲刺/考前 |
| weakPoints | 薄弱点 | "高数是弱项" / "英语阅读薄弱" |

提取函数：`extractAndSave(message)` — 从自然语言中正则匹配并自动保存

#### 学习记录

- 每条记录：日期、科目、时长（分钟）、内容、是否完成
- 自动提取："学了2小时高数" → `{ subject: "高数", duration: 120 }`
- 统计函数：今日/本周/累计时长、连续天数、各科分布

#### 计划数据

- 每日计划：日期 + 若干计划项（时间段、科目、任务、状态）
- 状态流转：pending → done / skipped
- 面板交互：点击切换状态

#### 记忆注入

`getMemoryContext()` 将档案、统计、今日计划拼成文本，随每条消息发送给后端，注入LLM上下文，实现个性化回复。

文件：[storage.ts](src/llm/memory/storage.ts)、[index.html](public/index.html)

### 3.3 考研计划生成

**流程**：

```
用户："帮我安排明天的英语复习"
  ↓
意图识别 → plan_create
  ↓
服务端调用LLM，Prompt要求返回JSON格式计划
  ↓
解析JSON → { date, items: [{time, subject, task, status}] }
  ↓
返回前端：文本回复 + 结构化planData
  ↓
前端保存到LocalStorage + 刷新计划面板
```

**计划生成规则**：
- 每天3-5个学习任务，每个1-2小时
- 上午精力好安排硬核科目（数学/专业课）
- 下午安排英语/政治
- 晚上安排复习巩固
- 考虑用户薄弱点和指定科目

**计划操作**：
- 创建：plan_create — LLM生成
- 查询：plan_query — 读取并格式化展示
- 修改：plan_modify — LLM修改后返回新计划
- 完成：plan_complete — 标记指定项done

文件：[plan.ts](src/llm/plan.ts)

### 3.4 考研知识问答

**流程**：

```
用户："什么是泰勒展开？"
  ↓
意图识别 → qa_ask
  ↓
服务端路由到QA处理器，构建专用Prompt
  ↓
调用LLM，返回考研难度讲解
  ↓
前端显示回复
```

**三种问答模式**：

| 子意图 | 触发示例 | 回答结构 |
|--------|---------|---------|
| qa_ask | "什么是泰勒展开？" | 概念定义 → 核心要点 → 考研例题 → 易错提醒 |
| qa_explain | "通俗解释一下中值定理" | 一句话理解 → 生活类比 → 考研实战 |
| qa_example | "出两道线性代数的题" | 2-3道递进难度题 + 详细解析 |

**个性化**：结合用户记忆上下文（目标院校、薄弱科目、备考阶段），针对性讲解。

文件：[qa.ts](src/llm/qa.ts)

### 3.5 考研提醒服务

**前端主动提醒**：页面打开时自动检查学习数据，以系统消息形式推送提醒。

| 提醒类型 | 触发条件 | 示例消息 |
|---------|---------|---------|
| 考试倒计时 | profile.examDate 存在 | "距离考研还有123天，坚持就是胜利！" |
| 今日未学习 | 今天无学习记录 | "今天还没开始学习哦，先从擅长的科目开始吧！" |
| 科目遗漏 | 某科3天+没学 | "高数已经3天以上没学了，今天安排复习一下吧" |
| 连续鼓励 | 连续学习≥3天 | "你已经连续学习7天了，太棒了！" |
| 计划提醒 | 有未完成计划 | "今天还有3项计划待完成，加油！" |

防重复机制：每种提醒独立追踪（`kaoyan-reminder-{type}-{date}`），互不阻塞，同一天每种提醒只显示一次。新用户引导使用独立的 `kaoyan-guide-shown` 标志，仅显示一次。

**后端 review 处理器**：

| 子意图 | 触发示例 | 功能 |
|--------|---------|------|
| review_query | "我高数哪些章节薄弱？" | 分析学习记录，指出薄弱科目 |
| review_schedule | "根据遗忘曲线安排政治复习" | 按1天/3天/7天/15天间隔安排复习 |
| review_stage | "现在该进入强化阶段了吗？" | 根据日期判断备考阶段，给出建议 |

文件：[review.ts](src/llm/review.ts)、[index.html](public/index.html)

### 3.6 学习数据统计

**展示方式**：右侧滑入统计面板（📊按钮切换），与计划面板（📋）互斥

**面板内容**：

| 区域 | 内容 | 数据来源 |
|------|------|---------|
| 备考进度 | 距考研天数、进度条、连续学习天数 | `profile.examDate`、`getStudyStats().streak` |
| 学习时长 | 今日/本周/本月小时数、累计天数 | `getStudyStats()` |
| 近7天趋势 | 折线图（Chart.js Line） | `getStudyRecords()` 按日聚合 |
| 四科分布 | 饼图（Chart.js Doughnut） | `getStudyStats().subjects` |
| 计划完成率 | 今日完成数、累计完成率 | `getPlans()` 统计 |

**图表库**：Chart.js 4.x（CDN引入），Chart.js 不可用时降级显示文字统计

**自动刷新**：面板打开时收到新消息自动更新数据

文件：[index.html](public/index.html)

### 3.7 考研场景Prompt

系统提示词定义了考研小管家的角色：
- 考研四科知识体系（政治/英语/数学/专业课）
- 四阶段备考节奏（基础→强化→冲刺→考前）
- 六大核心能力（学习记录、计划管理、知识问答、复习提醒、情绪关怀、进度统计）
- 场景路由：根据意图添加场景提示（如"用户在记录学习""用户可能有焦虑情绪"）

文件：[prompt.ts](src/llm/prompt.ts)

### 3.8 考研情绪关怀

**流程**：

```
用户："感觉来不及了"
  ↓
意图识别 → chat_comfort
  ↓
服务端路由到Emotion处理器，构建专用Prompt
  ↓
调用LLM，返回考研场景情绪关怀回复
  ↓
前端显示回复
```

**三种情绪模式**：

| 子意图 | 触发示例 | 回复策略 |
|--------|---------|---------|
| chat_comfort | "感觉来不及了""想放弃" | 共情认可 → 正常化 → 数据鼓励 → 具体建议 → 温暖收尾 |
| chat_encouragement | "坚持了60天了！" | 热情肯定 → 数据对比 → 连接目标 → 轻激励 |
| chat_chat | "考研真的好累啊" | 轻松研友式聊天，不讲大道理 |

**Prompt设计原则**：
- 语气像经历过考研的学长/学姐，温暖但不鸡汤
- 不否定用户情绪（不说"你不应该这样想"）
- 不制造更多焦虑（不说"时间真的不多了"）
- 用具体数字说话（"你已经坚持了X天"）
- 结合用户记忆上下文（目标院校、薄弱科目）个性化回复
- 严重情绪问题（"抑郁""绝望"）建议寻求专业帮助

文件：[emotion.ts](src/llm/emotion.ts)

### 3.9 DeepSeek API集成

- 端点：`https://api.deepseek.com/v1/chat/completions`
- 模型：`deepseek-chat`（OpenAI兼容格式）
- 认证：Bearer Token（环境变量 `DEEPSEEK_API_KEY`）
- 参数：temperature 0.7，max_tokens 2048
- 降级：API未配置时提示用户设置环境变量

文件：[deepseek.ts](src/llm/deepseek.ts)

---

## 四、前端界面

### 4.1 布局

- 紫色渐变主题（#667eea → #764ba2）
- 顶部：Logo + 标题 + 连接状态
- 左半边：聊天区域（消息列表 + 快捷操作 + 输入框）
- 右半边：侧边栏（今日计划 / 学习统计 Tab 切换）
- 消息气泡：用户靠右紫色，AI靠左白色，带头像和时间戳
- 移动端（<768px）：侧边栏隐藏，📋按钮打开全屏 overlay

### 4.2 交互

| 操作 | 效果 |
|------|------|
| 输入消息回车 | 发送到后端，显示"正在输入"动画 |
| 收到回复 | 显示AI气泡，自动滚动 |
| 右侧 Tab 切换 | 今日计划 / 学习统计 |
| 点击计划项 | 切换 pending → done → skipped |
| 计划编辑 | 修改时间/科目/任务、删除、添加新项 |
| 历史计划 | 底部区域，左右箭头切换日期查看 |
| 快捷操作 | 一键发送常用指令 |
| 连接断开 | 自动重连（5秒间隔） |
| 刷新页面 | 从LocalStorage恢复对话历史 |

### 4.3 设备身份

- 首次访问自动生成 `deviceId`（随机字符串）
- 存储在 `localStorage.kaoyan-butler-device-id`
- 无需注册登录，设备即身份

---

## 五、API协议

### WebSocket消息格式

**客户端 → 服务端**：
```json
{
  "type": "chat",
  "text": "帮我安排今天的复习",
  "deviceId": "device-xxx",
  "timestamp": 1777706696901,
  "memoryContext": "【用户档案】\n姓名: 小明\n...",
  "plansJson": "[{\"date\":\"2026-05-02\",\"items\":[...]}]"
}
```

**服务端 → 客户端**：
```json
{
  "type": "chat",
  "text": "今天的计划安排好了！",
  "timestamp": 1777706702112,
  "intent": "plan_create",
  "planData": {
    "date": "2026-05-02",
    "items": [
      { "time": "08:30-10:30", "subject": "高数", "task": "复习极限", "status": "pending" }
    ]
  }
}
```

### HTTP接口

- `GET /` — 前端页面
- `GET /health` — 健康检查（返回连接数和时间戳）

---

## 六、测试

### 6.1 意图识别单元测试

文件：[test-intent.ts](test-intent.ts)

覆盖：
- 第一层正则匹配：18个用例（问候、感谢、告别、帮助、否定、查看）
- 第三层安全检查：15个用例（身体不适、过度学习、考研焦虑、比较焦虑、自伤风险）

### 6.2 端到端测试

文件：[test-ws.ts](test-ws.ts)

覆盖：
- 连接测试、消息发送接收
- 各类意图的消息交互
- 计划创建和查询

### 6.3 测试方案

文件：[测试方案.md](测试方案.md)

40+测试用例，覆盖三层意图识别的完整场景。

---

## 七、运行方式

### 环境要求

- Node.js ≥ 18
- DeepSeek API Key

### 安装与启动

```bash
# 安装依赖
npm install

# 配置API Key
echo "DEEPSEEK_API_KEY=sk-xxx" > .env

# 启动开发服务器
npm run dev

# 访问
# http://localhost:3000
```

### 测试

```bash
# 意图识别测试
npx tsx test-intent.ts

# WebSocket端到端测试（需先启动服务）
npx tsx test-ws.ts
```

---

## 八、当前进度

| 阶段 | 内容 | 状态 |
|------|------|------|
| 阶段一 | 项目骨架 + DeepSeek集成 | ✅ 完成 |
| 阶段二 | 三层意图识别系统 | ✅ 完成 |
| 阶段三 | 长期记忆系统 | ✅ 完成 |
| 阶段四 | 考研计划生成 | ✅ 完成 |
| 阶段五 | 考研知识问答 | ✅ 完成 |
| 阶段六 | 考研提醒服务 | ✅ 完成 |
| 阶段七 | 学习数据统计 | ✅ 完成 |
| 阶段八 | 考研情绪关怀 | ✅ 完成 |
| 阶段九 | 交互优化与前端重构 | ✅ 完成 |
| 阶段十 | 功能整合测试 | 待开发 |
| 阶段十一 | 部署与汇报 | 待开发 |

---

## 九、后续计划

1. **考研知识问答增强**：结合用户专业课和目标院校，提供针对性知识讲解
2. **遗忘曲线提醒**：基于艾宾浩斯遗忘曲线，自动安排复习时间
3. **功能整合测试**：完整联调，修复边界情况
4. **公网部署**：部署到Vercel或Render，支持手机访问
